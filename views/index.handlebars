<h1>Burger Log</h1>
<div class="display">

    <div class="not-eaten">
        <ol>
            {{#each burgers}}
                {{#unless devoured}}
                    <li class="flex-item list-one">
                        {{burger_name}}
                        <button class="devour" data-id="{{id}}" data-devoured="{{devoured}}">
                            Devour
                        </button>
                    </li>
                {{/unless}}
            {{/each}}
        </ol>
    </div>

    <div class="eaten">
        <ol>
            {{#each burgers}}
                {{#if devoured}}
                    <li class="flex-item list-two">
                        {{burger_name}}
                        <button class="undevour" data-id="{{id}}" data-devoured="{{devoured}}">
                            Add to List
                        </button>
                    </li>
                {{/if}}
            {{/each}}
        </ol>
    </div>

</div>

<div class="burger-form">
    <h3>Enter a Burger</h3>
    <form>
        <input type="text" id="burger_name" name="burger_name" placeholder="Burger Name" required>
        <button type="submit">Submit</button>
    </form>
</div>